# isitsummerinswedenyet-reason

Run this project:

```
npm install
npm start
# in another tab
npm run webpack
```

After the Webpack compilation is done, fire up `static/index.html` in
your browser and you should be set.
